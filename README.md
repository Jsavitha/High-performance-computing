hpc done 2 tasks
Performance Comparison & Amdahl's Law: Incorporate the vector addition logic to run on both the CPU and GPU. 
Compare the execution times, and plot the results on a graph to verify Amdahl's Law.

GPU Matrix Multiplication: Implemented matrix multiplication on the GPU, transitioning the vanilla C++ logic we discussed in class into a CUDA kernel.

